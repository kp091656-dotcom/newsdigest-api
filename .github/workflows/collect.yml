name: Daily Market Data Collector

on:
  schedule:
    # ① 台灣 15:00（UTC 07:00）— TWSE 盤後立即可取
    - cron: '0 7 * * 1-5'
    # ② 台灣 16:30（UTC 08:30）— FinMind 法人/選擇權/融資券更新完畢
    - cron: '30 8 * * 1-5'
  workflow_dispatch:  # 允許手動觸發（不帶 mode 則全收）

jobs:
  collect:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '24'

      - name: Install dependencies
        run: npm install @supabase/supabase-js node-fetch

      - name: Collect and store market data
        env:
          SUPABASE_URL:         ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          VERCEL_API_BASE:      ${{ secrets.VERCEL_API_BASE }}
          # 依觸發時間自動判斷模式（腳本內讀取）
          COLLECT_MODE: ${{ github.event.schedule == '0 7 * * 1-5' && 'twse' || github.event.schedule == '30 8 * * 1-5' && 'finmind' || 'all' }}
        run: node .github/scripts/collect_market_data.js
